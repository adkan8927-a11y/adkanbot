# 스케줄 에이전트 가이드 (AGENTS.md)

> [!IMPORTANT]
> 본 프로젝트의 전체 시스템 아키텍처, 폴더 구조 및 데이터 흐름은 **반드시 [`docs/architecture.md`](file:///Users/adkan/adkan연구2/docs/architecture.md) 를 참고**해 주시기 바랍니다. 시스템 구조 변경 시 본 문서와 아키텍처 문서를 함께 업데이트해야 합니다.

이 문서는 `schedule check/agents/` 디렉토리에 존재하는 개별 데이터 수집 에이전트(크롤러/파서)들의 목록과 역할을 정의합니다. 모든 에이전트는 `schedule_orchestrator.py`에 의해 호출됩니다.

## 🤖 에이전트 목록 및 역할

### 1. 공시 및 실적 에이전트
- **`dart_agent.py`**: DART(금융감독원 전자공시시스템)에서 당일 접수된 기업들의 핵심 공시(유상증자, 무상증자, 합병, 분할 등)를 크롤링하여 주요 이벤트로 등록합니다.
- **`earnings_agent.py`**: 국내 기업들의 잠정 실적 발표 및 기업설명회(IR) 개최 일정을 추출합니다.
- **`cb_agent.py` (CB 백필링 에이전트)**: 1년 전에 발행된 전환사채(CB)/신주인수권부사채(BW) 공시를 역추적하여, 보호예수가 풀리는 당일의 잠재 매도 리스크(Overhang) 일정을 생성합니다.

### 2. 거시 경제 및 글로벌 에이전트
- **`fred_macro_agent.py`**: 미국 세인트루이스 연방준비은행(FRED) API 등을 활용하여 금리 발표(FOMC) 및 주요 거시 경제 지표 발표 일정을 수집합니다.
- **`rss_global_agent.py`**: PR Newswire 및 Google Alerts RSS를 파싱하여 글로벌 테크/바이오 기업들의 주요 학회 및 컨퍼런스 일정을 수집합니다.

### 3. 정부 정책 및 수주 에이전트
- **`rss_policy_agent.py`**: 과기부, 식약처, 산업부 등 국내 주요 정부 부처의 공식 보도자료 RSS를 모니터링하여 정책 발표 일정을 추출합니다.
- **`customs_agent.py`**: 관세청 수출입 동향 발표일(매월 1일, 11일, 21일 등)을 자동 산출하여 일정에 추가합니다.
- **`dapa_agent.py`**: 방위사업청 등의 수주 계약 정보를 모니터링하여 방산 섹터 일정을 수집합니다.
- **`assembly_agent.py`**: 국회 사무처 관련 법안 발의 및 본회의 일정을 크롤링합니다.

### 4. 증시 일정 및 보호예수 에이전트
- **`stock_market_agent.py`**: 공모청약, 신규상장, 선물/옵션 만기일(파생만기) 등 국내 주식 시장 자체의 캘린더 이벤트를 수집합니다.
- **`pdf_lockup_agent.py`**: 한국예탁결제원(KSD)에서 매월 말 배포하는 '의무보유등록 해제' 안내 보도자료(PDF)를 파싱하여, 다음 달 보호예수 해제 일정을 100%의 정확도로 추출합니다.


### 5. VIP 돌발 모멘텀 에이전트
- **`vip_momentum_agent.py`**: 구체적인 날짜가 지정된 강력한 호재성 테마/섹터 재료(VIP 모멘텀)를 웹에서 스크래핑하여 `vip_momentum_alerts.csv`에 독립적으로 기록합니다.

### 6. 유틸리티 및 키워드 최신화 에이전트
- **`static_calendar.py`**: 변하지 않는 고정된 정적 이벤트나 공휴일 데이터를 병합할 때 사용됩니다.
- **`keyword_manager.py` (동적 키워드 최신화 엔진)**: 뉴스 매칭 히트 트래킹(`keyword_analytics.csv`), 30일 무반응 키워드 가지치기(`--prune`), 당일 주도주/상한가 이슈 기반 로컬 Ollama LLM(`gemma4:e4b`) 신규 주도재료 추출(`--discover`), 안전 백업 동기화(`--sync`)를 총괄합니다.

### 7. 🧪 로컬 신규 수집 후보 에이전트 (베타 파이프라인)
- **`test_broker_reports.py` (B-1)**: 네이버 금융 리서치에서 '목표가 상향' 및 'Top Pick' 종목분석 리포트만 파싱하는 로컬 에이전트.
- **`test_bio.py` (A-1)**: FDA 공식 보도자료 RSS 및 식약처 보도자료에서 신약 승인(PDUFA) 및 임상 관련 일정을 추출하는 에이전트.
- **`test_asia_macro.py` (A-2)**: Investing.com 등에서 중국 LPR 대출우대금리 및 일본은행(BOJ) 금리 결정을 수집하는 에이전트.
- **`test_raw_materials.py` (B-3)**: OilPrice 및 CNBC Commodities RSS에서 유가, 원자재, 지정학적 리스크 모멘텀 가중치를 분석하는 에이전트.

---

## 🔒 보안, 뉴스 라우팅 및 표기 정제 규칙 (Common Guidelines)
1. **보안 API 키 관리**: 코드 내부 하드코딩 금지. 반드시 최상위 `.env` 파일에 기재하고 `load_dotenv`를 사용해야 합니다.
2. **Title-Only(제목 전용) 뉴스 라우팅 엔진**:
   - 기사 본문 요약문(Description)으로 인한 오탐(예: 유럽 증시 기사가 뉴욕 증시 키워드에 낚임, 대미 가스발전소가 배터리에 낚임)을 100% 방지하기 위해 1차/2차 수집 및 유사도 필터링을 **기사 제목(`Title-Only`)으로만 수행**합니다.
   - Title-Only 모드에서의 임베딩 코사인 유사도 골디락스 임계치는 **`0.50`**으로 적용됩니다.
3. **구체적 금액 수치(예: `29조원`) 명시 기사 우선 채택 룰**:
   - 유사 기사(유사도 70% 이상)가 존재할 경우, 새로 들어온 기사의 제목에 **구체적인 투자/수주 금액 수치(예: `29조원`, `1367억`, `58억`, `198억달러`)**가 포함되어 있고 기존 기사에는 없다면, **금액이 명시된 고가치 기사로 자동 교체**하여 최고가치 뉴스만 리포트에 수용합니다.
4. **대시보드 표기 정제**:
   - `[권리락] [종목명] 유상증자 권리락` ➔ `[종목명] 유상증자 권리락`
   - `[보호예수] 종목명 의무보유 해제 (주수)` ➔ `[종목명] 의무보유 해제 (주수)`
   - 데이터베이스(CSV) 원본은 그대로 보존하고, HTML 렌더링 시점에만 텍스트를 자동 가공합니다.
5. **GitHub Secrets 동기화 의무**: API 키(Gemini, Naver, DART 등) 변경 시 로컬 `.env`뿐만 아니라 GitHub 저장소의 `Secrets and variables -> Actions` 항목도 동일하게 업데이트해야 원격 액션(`exit code 1`) 장애를 방지할 수 있습니다. 자세한 리스트는 [`docs/architecture.md`](file:///Users/adkan/adkan연구2/docs/architecture.md)의 5절을 참고하세요.

---
각 에이전트는 독립적인 스크립트로 구성되어 있으며 단독 실행(`python3 agent_name.py`)을 통해 수집 결과를 테스트할 수 있습니다. 에이전트를 추가하거나 삭제할 때는 오케스트레이터(`schedule_orchestrator.py`)의 호출 로직도 함께 수정해야 합니다.
